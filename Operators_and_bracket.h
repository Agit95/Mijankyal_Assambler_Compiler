#pragma once

typedef enum
{
	plus = '+',
	minus = '-',
	division = '/',
	multiplication = '*',
	equal = '='

} E_Operators;


typedef enum
{
	left_bracket = '(',
	right_bracket = ')'

} E_Bracket;

